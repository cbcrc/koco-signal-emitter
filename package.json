{
  "name": "koco-signal-emitter",
  "version": "1.3.0",
  "homepage": "https://github.com/cbcrc/koco-signal-emitter",
  "authors": [
    "David D'Amico <david.damico@cbc.ca>"
  ],
  "description": "Wrapper for Miller Medeiros's Signal Emitter js-signals wrapper.",
  "main": "src/signal-emitter.js",
  "keywords": [
    "koco",
    "reactive"
  ],
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "license": "MIT",
  "dependencies": {
    "js-signals": "^1.0.0",
    "koco": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
